services:
  backend:
    build:
      context: ./cse_sync_back
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./cse_sync_back:/app
      - go-cache:/go/pkg/mod
    environment:
      - CGO_ENABLED=0
    networks:
      - cse-network

  frontend:
    build:
      context: ./cse_sync_front
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./cse_sync_front:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8080
    networks:
      - cse-network
    depends_on:
      - backend

volumes:
  go-cache:

networks:
  cse-network:
    driver: bridge
